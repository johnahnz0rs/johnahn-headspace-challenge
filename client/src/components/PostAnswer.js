import React from 'react';

const PostAnswer = props => {

    // const artists = props.favArtists;

    return (
        <React.Fragment>
            <span>lol this is PostAnswer</span>
        </React.Fragment>
    );

};

export default PostAnswer;
